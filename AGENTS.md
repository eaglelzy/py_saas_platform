# 企业级 SaaS 平台 MVP 行动纲要

## 产品愿景与用户角色
- **愿景**：以最小投入上线面向中小企业的账号与订阅管理平台，先满足租户入驻、权限控制、订阅升级三大核心路径。
- **角色**：平台管理员负责审核与运营；企业管理员负责租户初始化、成员与订阅管理；企业成员按授权访问业务功能。
- **假设**：线下支付可接受，套餐固定；通过手动审核、手动标记支付完成首批客户验证。

## 技术架构基线
- **后端**：FastAPI + SQLAlchemy + PostgreSQL（单库多租户，RLS 控制），Redis 维护 JWT 黑名单与限流计数，RabbitMQ 预留异步通知。
- **前端**：Angular 20 + TypeScript + Angular CLI（Standalone 组件 + NgRx 状态管理），划分租户控制台与平台运营台两套界面。
- **基础设施**：Docker Compose 本地全栈环境，GitHub Actions 执行 Lint/Test/Build；Prometheus + Grafana 模板化监控，Loguru 结构化日志。
- **部署策略**：单节点部署起步，保留横向扩展钩子（容器化、队列拆分）。

## 核心模块拆解
- **租户与成员管理**：租户入驻审批流；固定角色 `owner/admin/member`；成员邀请激活；套餐控制成员上限。
- **认证与授权**：邮箱密码登录（密码强度校验）；Access/Refresh JWT 搭配 Redis 黑名单；多租户上下文加载与 RLS；RBAC 权限表缓存 15 分钟。
- **订阅与升级**：预置免费/专业套餐；后台升级申请→线下收款→运营台标记生效；到期前 7 天/1 天提醒，逾期 7 天自动降级并记录审计。
- **审计与通知**：覆盖登录、成员/角色变更、套餐调整、支付确认等关键事件；审计数据落 PostgreSQL，可按租户/时间检索；RabbitMQ 触发站内消息与邮件发送。
- **平台运营台**：租户列表与详情、审批流程、支付标记、站内通知、订阅订单导出（CSV）。
- **支撑能力**：统一错误响应、日志包含 `tenant_id`/`user_id`、Redis/RabbitMQ 降级策略、每日备份策略。

## 研发角色分工（建议）
| 角色 | 主要职责 | 输出 |
| --- | --- | --- |
| 产品/项目 | 驱动需求澄清、维护路线图、验收核心流程 | PRD、原型、高优先级清单 |
| 后端 | 设计多租户模型、实现 API、RBAC、审计、队列占位 | FastAPI 微服务、数据库迁移、测试 |
| 前端 | 搭建租户端与运营端界面、表单校验、通知交互 | Angular 模块、UI 规范、E2E 验证脚本 |
| 运维/平台 | 构建 Docker Compose、CI/CD、监控、备份脚本 | Pipeline、监控仪表、部署指南 |

## 迭代节奏（6 周内）
| Sprint | 时间 | 目标 | 关键交付物 |
| --- | --- | --- | --- |
| Sprint 1 | 周 1-2 | 租户入驻、成员邀请、基础 RBAC、审计日志雏形、前端框架搭建 | FastAPI 核心域、入驻流程 UI、PostgreSQL RLS、JWT + Redis 黑名单 |
| Sprint 2 | 周 3-4 | 套餐配置、升级申请、手动支付链路、站内通知、基础监控 | 订阅 API、运营台升级界面、提醒任务、Prometheus/Grafana 基线 |
| Sprint 3 | 周 5-6 | RabbitMQ 异步通知、报表导出、限流配额、性能调优、上线清单 | 队列消费服务、CSV 导出、限流中间件、负载压测报告、运维文档 |

## 质量与验收基线
- **性能**：单节点 200 并发、P95 < 400ms；Redis 计数实现 API 配额。
- **安全合规**：HTTPS、PBKDF2 密码、JWT 安全管理、输入校验、操作二次确认；日志脱敏并标记租户上下文。
- **测试**：核心流程单元/集成测试覆盖 ≥60%；异步通知提供模拟测试；定期演练备份恢复。
- **交付物**：后端 API（`/api/v1`）、租户端/运营端前端、Docker Compose 栈、部署与运维文档、首批客户试运行报告。

## 风险与后续路线
- **当前风险**：RabbitMQ 依赖上线初期可能空转，需要降级策略；线下支付确认耗时；多租户数据隔离需严密测试。
- **优先级备选**：完成 MVP 后按顺序推进在线支付 → 自定义角色 → Webhook/第三方集成 → 自动计费 → 高级监控与告警 → SSO/企业认证。
